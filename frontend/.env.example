# 前端环境变量样例。用法：cp .env.example .env，按需填写后 npm run dev / build。
# Vite 只暴露以 VITE_ 开头的变量到前端代码（import.meta.env）。

# 受控动作鉴权 token（P0-4）：须与后端 .env 的 OPERATOR_TOKEN 一致。
# 留空 = 演示模式（后端未配 token 时本机可信控制台直接放行）。
VITE_OPERATOR_TOKEN=
