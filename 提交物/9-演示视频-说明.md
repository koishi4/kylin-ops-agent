# 9. 演示视频（待录制）

状态：⏳ **尚未录制**（roadmap 第 4 周该项未勾选）。

录制脚本 / 旁白逐字稿见 `docs/演示讲稿.md`，PPT 见同目录 `8-演示PPT.pptx`。

## 官方场景脚本（约 7 分钟）

按赛题官方演示场景串一条线，每段对应一个评分子项：

1. **清理系统垃圾** —— 一句话「帮我清理系统垃圾」→ Agent 选只读工具感知 → 列出可清理项（评分①②）
2. **发现大日志文件** —— 磁盘占用研判，定位大文件（评分④ 根因分析）
3. **识别关键性** —— 区分「关键/可清理/未知」，不误删（评分④）
4. **权限校验** —— 受控动作走 `/action/execute` 强制二次确认 + 非 root 最小权限（评分③）
5. **避免系统崩溃** —— 危险命令（如 `rm -rf /`）被护栏拦截 + 防注入演示 + 思维链回放（评分③ + 溯源闭环）

## 录制完成后

把视频文件（建议 `9-演示视频.mp4`，≤ 官方时长上限）放入本目录，
并在 `0-提交物清单.md` 把第 9 项状态从「⏳ 待录制」改为「✅」。
视频文件不入 git（体积大），随上传 zip 一并打包即可。
