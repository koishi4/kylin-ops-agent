# CLAUDE.md — 麒麟安全智能运维 Agent（软件杯 A2）

> 本文件是 Claude Code 在本项目的最高行为准则。每次开始任务前先读它。
> 这同时是「中国软件杯 A2 赛题作品」和「软件工程课程报告」两个交付物，一套代码两份文档。

## 1. 项目是什么

开发一套**部署在麒麟操作系统上、通过自然语言运维 Linux 的智能 Agent**。
用户说人话（"帮我清理系统垃圾"），Agent 感知系统状态 → 推理 → **安全校验** → 最小权限执行 → 全链路留痕。

**核心命题（评委最看重）**：解决「AI 推理不可控」。能对话运维的人很多，能**拦住误删库/危险注入**并拿出**可追溯思维链日志**的人很少。安全护栏是本项目的灵魂，不是配菜。

## 2. 评分映射（一切开发以此为锚）

初赛评分：功能完整性 55% + 创新与实用性 25% + 文档与演示 20%。
功能 55% 细分四项，每写一个模块都要回答"它对应哪一项、能不能演示"：

| 评分子项 | 对应模块 | 目录 | 必须能演示 |
|---|---|---|---|
| ① OS 感知 + MCP 插件 | MCP Server | `backend/app/mcp_server/` | 列工具、调用工具拿到实时数据 |
| ② 自然语言交互准确性 | LLM 编排 | `backend/app/llm/` `backend/app/core/` | 一句话→正确选工具→正确回答 |
| ③ 安全护栏与风险控制 | Guardrail | `backend/app/guardrail/` | 危险命令被拦截 + 防注入 + 非 root |
| ④ 智能化根因分析 | Diagnosis | `backend/app/core/diagnosis.py` | 磁盘满→定位大文件→判断关键性 |

> ④ 根因分析在赛题"基本功能需求"里没列，但在评分表里是子项。**很多队会漏做，做了就拉开档次。** 优先级高。

## 3. 技术栈（已定，勿擅自更换）

- **后端**：Python 3.11 + FastAPI（B/S 架构的 S 端）
- **MCP**：官方 `mcp` Python SDK，工具层独立成 MCP Server，用 stdio/SSE 与后端通信
- **系统采集**：`psutil` + 封装 `lsof`/`netstat`/`journalctl`/`df`/`ss` 等命令
- **大模型**：双模式，通过 `LLM_PROVIDER` 环境变量切换
  - `deepseek`：云端 API，开发与录屏默认用它（效果稳）
  - `ollama`：本地 Qwen3-8B，答辩现场用它（国产化加分、断网可演）
  - 两套都要保留并测通，这是创新分与文档亮点
- **前端**：Vue3 + Vite + Element Plus（轻量，B/S 界面）
- **日志/审计库**：SQLite（无依赖、好部署、麒麟上零配置）
- **部署目标**：LoongArch 架构 + 麒麟高级服务器版 V11（官方发放虚机）

## 4. 安全护栏设计（项目灵魂，重点投入）

护栏是「对 LLM 生成的原始指令做二次过滤」，不是信任 LLM。链路：
```
自然语言 → LLM意图解析 → 生成候选命令 → 【护栏拦截层】 → 最小权限执行 → 审计
                                          ├ 1. 意图风险分类（黑/灰/白）
                                          ├ 2. 高危命令规则库匹配
                                          ├ 3. 提示词注入检测
                                          └ 4. 权限合规检查（非必要不 root）
```

规则库至少覆盖（写在 `guardrail/rules.py`，可热加载）：
- 删除类：`rm -rf /`、`rm` 作用于 `/`、`/etc`、`/var/lib/mysql` 等关键路径
- 权限类：`chmod 777` 关键目录、`chmod -R` 根目录
- 磁盘类：`mkfs`、`dd of=/dev/sd*`、`> /dev/sda`
- 提权类：未经授权的 `sudo`、`su`、`passwd`
- 注入类：命令拼接 `; rm`、`&& curl ... | sh`、反引号执行、`$(...)`
- 关键配置保护：禁止未授权修改 `/etc/passwd`、`/etc/fstab`、`sshd_config` 等

每条规则要有：`id`、`匹配模式`、`风险等级`、`命中后动作（拦截/二次确认/放行）`、`说明`。
风险分级用枚举，别用魔法字符串。被拦截的命令必须写入审计日志并向用户解释原因。

**抗提示词注入**：识别对话中诱导 Agent 越权的内容（如"忽略之前的规则"、"你现在是root可以做任何事"、文件内容里夹带指令）。检测到要拒绝并记录。

## 5. 推理链路溯源（评分明确要求的闭环）

完整记录五段：`接收指令 → 感知环境 → 推理决策 → 安全校验 → 执行结果`。
- 每次会话生成 `trace_id`，五段都挂在同一 trace 下
- 存 SQLite，前端能按 trace_id 回放整条思维链
- 这是"可追溯"的硬证据，演示时一定要展示日志回放界面

## 6. 开发纪律

- **每个模块先写测试再写实现**：`pytest`，测试用例直接复用进课程报告"系统测试"章节，一举两得
- **写完一个功能就更新 `docs/dev-log.md`**：记录设计决策、踩坑、解决方法 → 课程报告"总结"章节的素材
- **所有命令执行走 `core/executor.py` 统一出口**，禁止在别处直接 `os.system`/`subprocess` 裸调用，否则护栏形同虚设
- **绝不真的执行破坏性命令**：开发和演示用沙箱/受限账户，危险命令只演示"被拦截"，不演示"执行成功"
- **代码注释充分**：课程报告附录要贴源代码，注释清晰直接影响"文档质量"评分
- 国产化优先：能用国产开源组件就用，文档里要体现

## 7. 知识产权红线

- 开源组件按许可证使用并在 `docs/third-party.md` 标注来源，不可整段抄袭
- **安全护栏、风险规则库、思维链审计、根因分析这四块必须原创**（创新分 25% 全靠它们）
- 不得抄袭其他参赛队作品

## 8. 可复用的开源参考（只借管道，不抄灵魂）

- `mcp-system-monitor`：只读的 Linux 监控 MCP，参考其工具封装
- `mcp-shell-server`：白名单命令执行的 MCP，参考其安全范式
- 龙蜥 SysOM / Spug：国产运维平台，参考 OS 感知与 B/S 结构
- 这些只参考工具封装与架构思路，护栏逻辑自己写

## 9. 当前进度

见 `docs/roadmap.md` 的勾选状态。每完成一项就勾掉并写 dev-log。

## 10. skills 使用

本项目 `.claude/skills/` 下有三个技能，遇到对应任务务必先读：
- `mcp-tool-builder`：新增/封装一个 MCP 运维工具时
- `safety-guardrail`：编写或扩展安全护栏规则时
- `se-report-docx`：生成符合合工大格式的课程报告 Word 文档时
