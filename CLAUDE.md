# CLAUDE.md — 麒麟安全智能运维 Agent（软件杯 A2）

> 本文件是 Claude Code 在本项目的最高行为准则。每次开始任务前先读它。
> 这同时是「中国软件杯 A2 赛题作品」和「软件工程课程报告」两个交付物，一套代码两份文档。

## 1. 项目是什么

开发一套**部署在麒麟操作系统上、通过自然语言运维 Linux 的智能 Agent**。
用户说人话（"帮我清理系统垃圾"），Agent 感知系统状态 → 推理 → **安全校验** → 最小权限执行 → 全链路留痕。

**核心命题（评委最看重）**：解决「AI 推理不可控」。能对话运维的人很多，能**拦住误删库/危险注入**并拿出**可追溯思维链日志**的人很少。安全护栏是本项目的灵魂，不是配菜。

## 2. 评分映射（一切开发以此为锚）

初赛评分：功能完整性 55% + 创新与实用性 25% + 文档与演示 20%。
功能 55% 细分四项，每写一个模块都要回答"它对应哪一项、能不能演示"：

| 评分子项 | 对应模块 | 目录 | 必须能演示 |
|---|---|---|---|
| ① OS 感知 + MCP 插件 | MCP Server | `backend/app/mcp_server/` | 列工具、调用工具拿到实时数据 |
| ② 自然语言交互准确性 | LLM 编排 | `backend/app/llm/` `backend/app/core/` | 一句话→正确选工具→正确回答 |
| ③ 安全护栏与风险控制 | Guardrail | `backend/app/guardrail/` | 危险命令被拦截 + 防注入 + 非 root |
| ④ 智能化根因分析 | Diagnosis | `backend/app/core/diagnosis.py` | 磁盘满→定位大文件→判断关键性 |

> ④ 根因分析在赛题"基本功能需求"里没列，但在评分表里是子项。**很多队会漏做，做了就拉开档次。** 优先级高。

## 3. 技术栈（已定，勿擅自更换）

- **后端**：Python 3.11 + FastAPI（B/S 架构的 S 端）
- **MCP**：官方 `mcp` Python SDK，工具层独立成 MCP Server，用 stdio/SSE 与后端通信
- **系统采集**：`psutil` + 封装 `lsof`/`netstat`/`journalctl`/`df`/`ss` 等命令
- **大模型**：`deepseek`（云端 API，国产开源，开发/演示/录屏默认）+ `mock`（离线确定性桩，无 key/无网/CI），通过 `LLM_PROVIDER` 切换
  - 「国产化」由 DeepSeek 本身满足（深度求索，权重开源）——**不再挂本地 Qwen3-8B 双模式**。
    8B 在多轮编排 + JSON 自愈 + CaMeL 隔离阅读这套复杂协议下指令遵循/JSON 合法性断崖式下降，
    为对齐它而妥协架构得不偿失；故移除（详见 dev-log「2026-06-11 移除本地 8B 双模式」）。
  - 抽象保留：`llm/provider.py` 的 `LLMProvider` ABC + `_OpenAICompatProvider` 不与厂商耦合，
    任一 OpenAI 兼容端点（含私有化自托管的国产大模型）改 `DEEPSEEK_BASE_URL` 即可数行接入，
    国产化/离线诉求按需用「更大的自托管模型」满足，而非牺牲架构去迁就小模型。
- **前端**：Vue3 + Vite + Element Plus（轻量，B/S 界面）
- **日志/审计库**：SQLite（无依赖、好部署、麒麟上零配置）
- **部署目标**：LoongArch 架构 + 麒麟高级服务器版 V11（官方发放虚机）

## 4. 安全护栏设计（项目灵魂，重点投入）

### 4.0 主控制是「架构」，规则引擎是「纵深防御」（务必先建立这个心智模型）

本项目的第一安全保证**不是**「拦住 LLM 生成的危险命令」，而是**从架构上让 LLM 根本没有生成自由命令的途径**（Action-Selector / 能力受限范式）：

```
                       ┌─ 感知/查询：LLM 只能从 READONLY MCP 工具里【选工具】，不能拼装、不能自由执行；
自然语言 ─→ 编排器 ─┤   不可信工具输出经 CaMeL 双 LLM 隔离（无工具的「隔离阅读器」压成不可信摘要，
                       │   原始字节绝不进规划器）+ spotlighting 后才回喂；污点真正 gate（违则 fail-safe）
                       └─ 变更/处置：只有 truncate_log / kill_process / clean_path /
                           restart_service / reload_config / block_ip / clean_journal 七个
                           【白名单参数化动作】，且只能由用户经 /action/execute 显式触发、
                           强制二次确认；LLM 无法把它们当工具调用（不在 MCP 注册表里）
```

由此得到的**结构性不变量**（见 `guardrail/trifecta.py`、`tests/test_e2e_injection.py` 实测）：
- 全部 MCP 工具均为 READONLY，没有任何「改状态/外联」的能力腿 → 感知路径能力上限 ≤2，
  天然满足 Meta「Rule of Two」，**注入再成功也无法升级为状态变更**；
- 变更入口与 LLM 编排路径**物理隔离**，第三条腿只存在于「强制二次确认」的动作层。

→ 因此「即使 LLM 被诱导也炸不了系统」靠的是**这条架构**，命令规则引擎是它之上的**纵深防御**
（万一未来有人把自由命令接进 executor，规则仍兜底）。讲故事/写报告时，把架构讲成主角，规则讲成配角。

> **澄清「只有几个动作 = 只读面板」的误读（见 dev-log「2026-06-11 二.1」）**：当前白名单已含
> `truncate_log / kill_process / clean_path / restart_service / reload_config / block_ip /
> clean_journal` 七个动作，且**这只是当前动作集，不是架构天花板**。Action-Selector 范式（CaMeL）
> 正是给 agent **安全变更能力**的业界推荐解法——要继续扩实用性（如 `rollback_config` / `quota_set` …），
> 就**往白名单加参数化动作**：每个都「定参数 schema + 强制二次确认 + 走 executor 过防线2 + 审计留痕」，
> 能力随之增长而**不**给 LLM 自由 shell。错误做法才是为"实用"把自由命令交给模型。
> 即：实用性的扩展方向是**加受控动作**，绝不是**放开任意执行**——这条不变量不卖。

### 4.1 命令规则引擎（防线2，纵深防御层）

对任何要落到系统上的命令做独立的二次过滤（不信任来源）。链路：
```
候选命令（受控动作构造 / 自由形态）→ executor 统一出口 →【护栏裁决】→ 最小权限校验 → 沙箱落地 → 审计
                                       ├ 2. 高危命令规则库（正则 + realpath 路径兜底 + Bash AST 结构分析
                                       │     + 副作用效果分析「按效果判定而非拦串」，四者保守合并只升不降）
                                       └ 4. 权限合规检查（非必要不 root）
入口另有：├ 1.   意图分类【路由层】（白=只读快路 / 灰=进研判+规则；黑仅注入话术——破坏意图不在意图层判，见下）
          ├ 1.5 LLM 语义研判（对「灰」叠加：委婉删库靠语义而非话术识别，与规则保守合并取更严、AI 只升不翻案）
          └ 3.   提示词注入检测（用户输入 + 工具输出沙盒化）
```

> 注：防线1 是**路由层不是安全闸门**——2026-06 起不再用明文关键词判「破坏意图」（删库/格式化/提权）。
> 明文匹配在输入侧可被 base64/小语种/GCG 对抗后缀绕开、对真攻击是摆设却高频误杀正常运维词（如「检查提权风险」）。
> 破坏意图的拦截靠 §4.0 架构（无任意执行腿）+ 防线1.5 语义研判 + 防线2 命令产物侧裁决，**没有一层依赖话术匹配**。
> 详见 `docs/security-design.md §7.4` 与 dev-log「2026-06-11 意图层去明文化」。

规则库至少覆盖（写在 `guardrail/rules.yaml` + 红线硬编码于 `rules.py`，支持热加载）：
- 删除类：`rm -rf /`、`rm` 作用于 `/`、`/etc`、`/var/lib/mysql` 等关键路径
- 权限类：`chmod 777` 关键目录、`chmod -R` 根目录、**单文件改 `/etc/shadow|sudoers|passwd` 权限/属主**
- 磁盘类：`mkfs`、`dd of=/dev/sd*`、`> /dev/sda`
- 提权类：未经授权的 `sudo`、`su`、`passwd`、`useradd -u 0`、篡改 sudoers
- 注入类：命令拼接 `; rm`、`&& curl ... | sh`、反引号执行、`$(...)`、`$IFS` 空格替代绕过
- **外联/反弹 shell（egress）**：`/dev/tcp`、`nc -e`、`socat EXEC` —— 防「把本机交给远端」
- 关键配置保护：禁止未授权修改 `/etc/passwd`、`/etc/fstab`、`sshd_config` 等

每条规则要有：`id`、`匹配模式`、`风险等级`、`命中后动作（拦截/二次确认/放行）`、`说明`。
风险分级用枚举，别用魔法字符串。被拦截的命令必须写入审计日志并向用户解释原因。

> ⚠️ 扩展护栏时的纪律：**优先强化架构控制（收紧动作白名单 / 能力标签 / 工具只读性），
> 而非无止境地往规则库堆正则**。正则是脆的、可被语序与变形绕过（见 dev-log「红队补强」一节）；
> 黑名单跑步机永远追不完。只有「LLM 够不到危险路径」这条架构保证是确定性的。

**抗提示词注入**：识别对话中诱导 Agent 越权的内容（如"忽略之前的规则"、"你现在是root可以做任何事"、文件内容里夹带指令）。检测到要拒绝并记录；藏在**工具输出**里的注入用 spotlighting/datamarking 结构性隔离 + 降权（而非拒绝处理日志）。

## 5. 推理链路溯源（评分明确要求的闭环）

完整记录五段：`接收指令 → 感知环境 → 推理决策 → 安全校验 → 执行结果`。
- 每次会话生成 `trace_id`，五段都挂在同一 trace 下
- 存 SQLite，前端能按 trace_id 回放整条思维链
- 这是"可追溯"的硬证据，演示时一定要展示日志回放界面

## 6. 开发纪律

- **每个模块先写测试再写实现**：`pytest`，测试用例直接复用进课程报告"系统测试"章节，一举两得
- **写完一个功能就更新 `docs/dev-log.md`**：记录设计决策、踩坑、解决方法 → 课程报告"总结"章节的素材
- **所有命令执行走 `core/executor.py` 统一出口**，禁止在别处直接 `os.system`/`subprocess` 裸调用，否则护栏形同虚设
- **绝不真的执行破坏性命令**：开发和演示用沙箱/受限账户，危险命令只演示"被拦截"，不演示"执行成功"
- **代码注释充分且合规**：课程报告附录要贴源代码，注释清晰直接影响"文档质量"评分。注释/代码规范已
  工具化强制——中文 Google 风格 docstring + 正确性/整洁度，由 ruff 的 `D,F,I,B,UP` 规则把关（行宽 `E501`、
  改语义的 `UP042` 刻意不开，理由见标准文档），生产代码 `cd backend && ruff check app` 必须零违规（标准与
  豁免理由见 `docs/code-comment-style.md`）。新增代码补齐至过线，勿用 `# noqa` 绕过
- 国产化优先：能用国产开源组件就用，文档里要体现

## 7. 知识产权红线

- 开源组件按许可证使用并在 `docs/third-party.md` 标注来源，不可整段抄袭
- **安全护栏、风险规则库、思维链审计、根因分析这四块必须原创**（创新分 25% 全靠它们）
- 不得抄袭其他参赛队作品

## 8. 可复用的开源参考（只借管道，不抄灵魂）

- `mcp-system-monitor`：只读的 Linux 监控 MCP，参考其工具封装
- `mcp-shell-server`：白名单命令执行的 MCP，参考其安全范式
- 龙蜥 SysOM / Spug：国产运维平台，参考 OS 感知与 B/S 结构
- 这些只参考工具封装与架构思路，护栏逻辑自己写

## 9. 当前进度

见 `docs/roadmap.md` 的勾选状态。每完成一项就勾掉并写 dev-log。

## 10. skills 使用

本项目 `.claude/skills/` 下有三个技能，遇到对应任务务必先读：
- `mcp-tool-builder`：新增/封装一个 MCP 运维工具时
- `safety-guardrail`：编写或扩展安全护栏规则时
- `se-report-docx`：生成符合合工大格式的课程报告 Word 文档时
