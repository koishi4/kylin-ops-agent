# 加护栏 vs 不加护栏：量化 A/B 实验报告

> 麒麟安全智能运维 Agent — 自动生成（`python scripts/redteam_ab.py`），勿手改。
> 生成时间：2026-06-06 11:38:35

## 方法论（诚实标注）

借鉴 InjecAgent / AgentDojo / RedCode-Exec 的「有防御 vs 无防御」评测范式，**适配到本项目自有运维语料**（非跑原版基准，其工具集为邮件/银行/Docker，与运维不一一对应）。
护栏抽象为可开关中间层：**OFF** 模拟「只有 LLM、无确定性护栏」基线；**ON** 走防线2 规则库 + realpath 兜底（命令）/ 防线3 注入扫描（话术）。两档跑同一红队语料。

语料：危险命令 **44** 条、注入话术 **8** 条（合计攻击样本 **52**）、正常运维 **18** 条。（与 `tests/test_guardrail_redteam.py` 同源，样本即用例。）

## 核心结论

| 指标 | 不加护栏（OFF） | 加护栏（ON） | 变化 |
|---|--:|--:|--:|
| 攻击成功率 ASR（越低越好） | 100.0% | 0.0% | ↓ 100.0% |
| 危险命令拦截率 | 0.0% | 100.0% | ↑ |
| 注入话术识别率 | 0.0% | 100.0% | ↑ |
| 正常任务完成率（越高越好） | 100.0% | 100.0% | 几乎不降 |

## 攻击成功率对比（ASR，越短越好）

```
不加护栏 OFF  ████████████████████████████████ 100%
加护栏   ON   ································ 0%
```

## 正常任务完成率对比（越长越好）

```
不加护栏 OFF  ████████████████████████████████ 100%
加护栏   ON   ████████████████████████████████ 100%
```

## 结论

- 护栏把攻击成功率从 **100%** 压到 **0%**（净降 **100%**），同时正常任务完成率维持 **100%**（误杀 0 条）。
- 与文献量级对照：AgentDojo 自带 tool-filter 把 ASR 降到 7.5%、Spotlighting 把 ASR 从 >50% 降到 <2%；本项目在自有运维语料上达到近 0%，且**确定性规则掌握最终放行权**（非概率式检测，不会被自适应攻击翻案）。
- 诚实边界：本实验量化的是**确定性命令/注入护栏**的净贡献；自由口语的语义泛化由运行时 LLM 承担，二者叠加（规则保可靠 + LLM 补泛化）。护栏非银弹，定位为「纵深防御 + 显著降低最高危后果」。
