@startuml principle-diagnosis
!include _style.puml
title 附图4　大文件「能不能删」判定（根因分析）

start
:定位大文件（消费只读感知数据）;
:取「字面路径 ∪ 软链解析后真实路径」;
if (命中关键特征?\n/var/lib/mysql…  或  .ibd/.db/.sql/.mdf…) then (是)
  :分类 = 关键·勿删;
  :理由：删除不可逆，禁止贸然清理;
elseif (命中可清理特征?\n日志/缓存/临时  或  .log/.tmp/.cache…) then (是)
  :分类 = 可清理;
  :理由：通常可安全清理（先确认无进程占用）;
else (都不命中)
  :分类 = 拿不准;
  :理由：建议人工确认用途;
endif
:输出 分类 + 理由（证据），仅给建议;
:绝不自行删除——清理仍须走受控动作 + 二次确认;
stop

note right
  判定优先级：关键 > 可清理 > 未知；
  关键优先，宁可保守也不误导用户删数据。
  跨信号关联根因（磁盘+IO 双告警）另给
  证据链 + 置信度（各信号权重累加，和为 1）。
end note
@enduml
